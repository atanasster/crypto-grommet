import React from 'react';
// import Flag as WorldFlag from 'react-world-flags';

// eslint-disable-next-line no-unused-vars
export default ({ code, height = 12 }) => (<div>{code}</div>);

// export default ({ code, height = 12 }) => (<Flag code={code} height={height}/>);
